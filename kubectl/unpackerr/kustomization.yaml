apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml

secretGenerator:
  - name: unpackerr-secrets
    envs:
      - env-vars.env

generatorOptions:
  disableNameSuffixHash: true